<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cocktail_dakk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cocktail_dakk" >

        <activity android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.search_tab.SearchTabActivity">
        </activity>

        <activity android:name=".ui.menu_detail.MenuDetailActivity">
        </activity>

        <activity android:name=".ui.menu_detail.MenuDetailEvaluateActivity">
        </activity>


        <activity
            android:name=".ui.start.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">


        </activity>
        <activity android:name=".ui.start.StartNameActivity" />
        <activity android:name=".ui.start.StartActivity" />
        <activity android:name=".ui.start.StartSettingActivity" />

    </application>

</manifest>